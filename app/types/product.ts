export type product = {
    id: string,
    imageURL: string,
    price: number,
    title: string,
    description: string,
    colors: Array<string>,
    sizes: Array<string>,
    more: Array<string>
}